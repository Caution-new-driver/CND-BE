spring:
  application:
    name: cndbe-test
  datasource:
    url: jdbc:h2:mem:cndbe;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

openai:
  api-key: test-key

cloudinary:
  cloud-name: test-cloud
  api-key: test-key
  api-secret: test-secret

app:
  auth:
    access-password: test-password
    session-secret: test-session-secret
